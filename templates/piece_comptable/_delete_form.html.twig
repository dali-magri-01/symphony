<form method="post" action="{{ path('app_piece_comptable_delete', {'id': piece_comptable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece_comptable.id) }}">
    <button class="btn">Delete</button>
</form>
