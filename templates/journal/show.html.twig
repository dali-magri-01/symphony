{% extends 'base.html.twig' %}

{% block title %}Journal{% endblock %}

{% block body %}
    <h1>Journal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ journal.id }}</td>
            </tr>
            <tr>
                <th>Jl_code</th>
                <td>{{ journal.jlCode }}</td>
            </tr>
            <tr>
                <th>Jl_lib</th>
                <td>{{ journal.jlLib }}</td>
            </tr>
            <tr>
                <th>Mois_exer</th>
                <td>{{ journal.moisExer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_journal_index') }}">back to list</a>

    <a href="{{ path('app_journal_edit', {'id': journal.id}) }}">edit</a>

    {{ include('journal/_delete_form.html.twig') }}
{% endblock %}
