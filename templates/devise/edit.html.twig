{% extends 'base.html.twig' %}

{% block title %}Edit Devise{% endblock %}

{% block body %}

    {{ include('devise/_form.html.twig', {'button_label': 'Update'}) }}

{#    <a href="{{ path('app_devise_index') }}">back to list</a>#}

    {% endblock %}
